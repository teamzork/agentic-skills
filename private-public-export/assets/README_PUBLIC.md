# Project Name

Short, public-friendly description of the project.

## Setup

1. Clone and enter the repo:
```bash
git clone https://github.com/youruser/yourrepo.git
cd yourrepo
```

2. Create and activate a virtual environment:
```bash
python -m venv .venv
source .venv/bin/activate  # On Windows: .venv\Scripts\activate
```

3. Install dependencies:
```bash
pip install -r requirements.txt
```

## Usage

Add the minimal commands needed to run locally.

## Notes

- Runtime directories (credentials, data, logs) are created locally and are not versioned.
- Public repo contains source only; no private data is included.
